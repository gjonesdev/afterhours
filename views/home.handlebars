<ul>
    {{#each bars}}

    <li>
        <div class="row"></div>
        <a href="/marvelcharacter/{{_id}}">
            <div class="card">
                {{name}} <br>
                {{location.city}} <br>
                {{reviewsAverage}} <br>
                {{reviewsCount}} reviews <br>
                {{likesCount}} likes <br>
            </div>
        </a>
        </div>
    </li>

    {{/each}}
</ul>